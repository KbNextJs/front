export default function RootLayout({ children }) {
    return (
        <html lang="en">
            <head>
                <title>직무 소개</title>
            </head>
            <body>{children}</body>
        </html>
    );
}
