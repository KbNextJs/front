export default function RootLayout({ children }) {
    return (
        <html lang="en">
            <head>
                <title>자원 봉사</title>
            </head>
            <body>{children}</body>
        </html>
    );
}
