export default function RootLayout({ children }) {
    return (
        <html lang="en">
            <head>
                <title>자원봉사 신청</title>
            </head>
            <body>{children}</body>
        </html>
    );
}
