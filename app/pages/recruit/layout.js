export default function RootLayout({ children }) {
    return (
        <html lang="en">
            <head>
                <title>채용시스템</title>
            </head>
            <body>{children}</body>
        </html>
    );
}
