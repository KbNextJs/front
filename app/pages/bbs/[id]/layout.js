export default function RootLayout({ children }) {
    return (
        <html lang="en">
            <head>
                <title>식단표</title>
            </head>
            <body>{children}</body>
        </html>
    );
}
