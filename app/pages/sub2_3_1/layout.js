export default function RootLayout({ children }) {
    return (
        <html lang="en">
            <head>
                <title>상담 신청</title>
            </head>
            <body>{children}</body>
        </html>
    );
}
