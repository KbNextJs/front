export default function RootLayout({ children }) {
    return (
        <html lang="en">
            <head>
                <title>인재상</title>
            </head>
            <body>{children}</body>
        </html>
    );
}
