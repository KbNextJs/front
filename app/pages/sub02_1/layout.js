export default function RootLayout({ children }) {
    return (
        <html lang="en">
            <head>
                <title>sitemap 01</title>
            </head>
            <body>{children}</body>
        </html>
    );
}
