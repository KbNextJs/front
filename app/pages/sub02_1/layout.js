export default function RootLayout({ children }) {
    return (
        <html lang="en">
            <head>
                <title>위례빌리지 소개</title>
            </head>
            <body>{children}</body>
        </html>
    );
}
