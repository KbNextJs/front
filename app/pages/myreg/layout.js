export default function RootLayout({ children }) {
    return (
        <html lang="en">
            <head>
                <title>사업장 안내</title>
            </head>
            <body>{children}</body>
        </html>
    );
}
